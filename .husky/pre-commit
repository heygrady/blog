# https://typicode.github.io/husky/how-to.html#ci-server-and-docker
[ -n "$CI" ] && exit 0

branch="$(git rev-parse --abbrev-ref HEAD)"

if [ "$branch" = "main" ]; then
  echo "You can't commit directly to main branch"
  exit 1
fi

yarn biome check --staged --no-errors-on-unmatched
